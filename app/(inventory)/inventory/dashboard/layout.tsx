import InventoryLayout from "@/components/layouts/InventoryLayout";

export default function InventoryDashboardLayout({ children }: { children: React.ReactNode }) {
  return <InventoryLayout>{children}</InventoryLayout>;
}
